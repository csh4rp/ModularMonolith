name: build-project
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Setup .NET 8.x
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '8.x'
    - name: Install dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build